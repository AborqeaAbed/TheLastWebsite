spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/thelastwebsite
    username: ${USER:postgres}
    password:
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

stripe:
  secret-key: sk_test_your_stripe_secret_key_here
  publishable-key: pk_test_your_stripe_publishable_key_here
  webhook-secret: whsec_your_webhook_secret_here

email:
  from: hello@thelastwebsite.com
  magic-link-base-url: http://localhost:3000

logging:
  level:
    com.thelastwebsite: DEBUG